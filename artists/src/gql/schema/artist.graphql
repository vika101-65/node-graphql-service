type Artist @key(fields: "id") {
    id: ID!
    firstName: String
    secondName: String
    middleName: String
    birthDate: String
    birthPlace: String
    country: String
  
}

type Query {
  artist(id:ID!): [Artist]
}