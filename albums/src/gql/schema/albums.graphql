 type Album @key(fields: "id"){
   id: ID!
   name: String
   released: Int
   image: String
   
 }

 type Query {
   album(id: ID!): Album
   albums(limit:Int offset:Int, filter:String): [Album] 
 }
